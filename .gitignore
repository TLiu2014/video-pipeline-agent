node_modules/
dist/
build/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env (keep .env.example tracked)
.env
.env.local
.env.gcp
.envrc
.npmrc

# media workspace — staged sources (/public/media) + pipeline run outputs
# (/public/renders). Both hold heavy generated/loaded files; keep them untracked.
/media/
/public/media/
/public/renders/
*.mp4
*.mov
*.mkv
*.webm
*.mp3
*.wav
*.srt
*.vtt
# ...but DO allow the bundled sample clips + folder scaffolding to be committed.
!/public/samples/
!/public/samples/**

# misc
.DS_Store
*.log
.vite/
